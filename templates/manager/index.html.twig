{% extends 'base.html.twig' %}

{% block title %}Hello ManagerController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <table>
        <tr>
            <th>Tickets Open</th>
            <th>Tickets InProgress</th>
            <th>Tickets Waiting For Feedback</th>
            <th>Tickets Closed</th>
        </tr>
        <tr>
            <td>{{ openTickets }}</td>
            <td>{{ inProgressTickets }}</td>
            <td>{{ feedbackTickets }}</td>
            <td>{{ closedTickets }}</td>
        </tr>
    </table>
    <a href="{{ path('ticket_deassign') }}">Deassign all tickets</a>
{% endblock %}

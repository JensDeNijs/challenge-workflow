<nav>
    <ul>
        {% if is_granted('ROLE_USER')%}
        <li>
            <a href="{{ path('_profiler_home') }}">Homepage</a>
        </li>
        <li>
            <a href="{{ path('app_logout') }}">Logout</a>
        </li>

        <li>
            <a href="{{ path('ticket_index') }}">My Tickets</a>
        </li>
        {% else %}
            <li>
                <a href="{{ path('app_login') }}">Login</a>
            </li>
        {% endif %}
        {% if is_granted('ROLE_AGENT')%}
            <li>
                <a href="{{ path('_profiler_phpinfo') }}">All Tickets</a>
            </li>
        {% endif %}
    </ul>
</nav>